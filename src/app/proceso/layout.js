export const metadata = {
  title: "Proceso | S2 Project | Como trabajamos",
  description: "Asi trabajamos en S2 Project: diagnostico, planificacion, produccion y crecimiento. Un solo servicio, todo integrado.",
};

export default function ProcesoLayout({ children }) {
  return <>{children}</>;
}
